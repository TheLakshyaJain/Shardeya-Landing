/**
 * Broker tenant profile: BR-01..BR-08. Must never depend on {@code com.shardeya.builder}
 * (ArchUnit-enforced) — cross-profile needs are lifted into {@code foundation}.
 */
package com.shardeya.broker;
