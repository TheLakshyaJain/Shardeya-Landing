# Backend API base URL. The backend isn't in docker-compose's frontend
# service yet (M1 only added it there for the API itself), so `npm run dev`
# talks to it directly at this address — no Vite proxy configured.
VITE_API_BASE_URL=http://localhost:8080/api/v1
